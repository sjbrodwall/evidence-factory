---
alwaysApply: true
---

# Evidence Factory — Workflow

**Follow AGENTS.md (repo root).** Branch per task; 5-step response (goal → files → minimal change → diff → tests); diff summary + verification; CI is source of truth.
